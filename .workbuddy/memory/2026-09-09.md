# 2026-09-09

- 配置初始化：将 stardust_ai/.env.example 与 stardust_springboot/.env.example 复制为各自的真实 .env 并填入用户提供的值（MySQL 本机、阿里云 MaaS OpenAI 兼容端点、qwen3.7-flash、双端一致内部令牌、JWT 密钥、随机生成的 refresh pepper）。密钥值不记录。
- Embedding 模型：双端统一使用 `qwen3.7-text-embedding`（用户指定；阿里云端点无 text-embedding-3-small）。
- Spring Boot 不自动读取 .env，需通过环境变量注入后启动（`set -a && source .env && set +a` 或 IDE 环境变量）。
- stardust_springboot/.gitignore 已补 `### Environment / Secrets ###` 段：忽略 .env / .env.local / .env.*.local，用 `!.env.example` 保留模板可提交；git check-ignore 已验证 .env 被忽略、.env.example 仍可跟踪。
- 第二轮启动失败：`Failed to determine a suitable driver class`（DataSource url 缺失）。同源：用户只手工补了 JWT/pepper，DB_URL/DB_USERNAME/DB_PASSWORD 仍未注入。已确认 MySQL 3306 在监听、mysql-connector-j 在 classpath、application.properties 未改动。
- 根治方案（已实施）：新增 `com.example.stardust_springboot.env.DotenvEnvironmentPostProcessor`（实现 `org.springframework.boot.EnvironmentPostProcessor`，注意 Boot 4.1 里 `org.springframework.boot.env.EnvironmentPostProcessor` 已标记待删除）+ `src/main/resources/META-INF/spring.factories` 注册键 `org.springframework.boot.EnvironmentPostProcessor`。启动自动读取工作目录 `.env`，addLast 最低优先级，真环境变量/系统属性/application.properties 优先；文件不存在则静默跳过。已用 javac+临时 main 验证解析：DB_URL 94 字符、pepper 64、jwt secret 64 全部 OK。
- 第三轮启动失败：DB 已连上、Flyway 已跑，卡在 `Found non-empty schema(s) stardust but no schema history table`。库 `stardust` 由根目录 `stardust_ai_chat_dev_mysql8.sql` 导入（19 张表 + 20 行演示数据），无 `flyway_schema_history`。已核对：dump 的 19 个表名与 V1~V9 迁移脚本产出的表**完全一致**，即 dump == V9 终态。修复方案（未代改，仅给出）：`spring.flyway.baseline-on-migrate=true` + `spring.flyway.baseline-version=9`。注意 baseline-version 必须设 9，默认 1 会导致 V2~V9 重跑报 table exists。风险点：`spring.jpa.hibernate.ddl-auto=validate` 若 dump 列定义与实体不一致会报错。
- 环境备注：本机 mvn 脚本报 "找不到 org.codehaus.plexus.classworlds.launcher.Launcher"（MAVEN_HOME 未生效），暂时用 javac 手工编译验证，必要时改用 mvn.cmd。
- Python 端流式聊天 404：请求打到了 `https://<endpoint>/chat/completions`（HTTP 404 → PROVIDER_REQUEST_REJECTED，retryable=False）。原因：`stardust_ai/.env` 的 `STARDUST_AI_OPENAI_COMPATIBLE_BASE_URL` 只写了域名，缺 `/compatible-mode/v1` 前缀；provider 用 `base_url.rstrip("/") + "/chat/completions"` 拼接。已修正为 `https://llm-my6w7pmu24pasmhk.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`，curl 实测 chat/completions 与 embeddings 均 200；`qwen3.7-text-embedding` 与 `text-embedding-v3` 在该端点都可用。教训：阿里云 MaaS 专属端点的兼容地址必须带 `/compatible-mode/v1`，DashScope 原生是 `/api/v1`。
- 404 仍未解决的第二层原因（真凶）：模型名来自 **MySQL `ai_model.external_model_id`**，不是 .env。dump 种子数据里是 `gpt-4.1-mini`，阿里云端点上不存在 → `{"error":{"code":"model_not_found","message":"Model not exist."}}` HTTP 404。已执行 SQL：`UPDATE ai_model SET external_model_id='qwen3.7-flash', display_name='Qwen3.7 Flash' WHERE id=1;` 并同步把 `ai_provider.base_url` 改为阿里云兼容地址。端到端 curl 本地 `/internal/chat/stream`（头 `X-Service-Authorization`）已返回 start/reasoning/delta 流。教训：改 .env 的模型默认值只影响"未指定模型"的兜底，实际聊天走的是 DB 里的模型行；Java 端无默认模型配置项，模型完全由 DB 决定。
- 无害告警：`No supported WebSocket library detected` —— uvicorn 0.52.4 未装 websockets/wsproto，且项目代码无任何 ws 路由，来自外部探测；如需 ws 再装 `uvicorn[standard]`。
- 观察项：第二次启动日志 `Found 0 JPA repository interfaces`（第一次是 19），src 与 target/classes 里都是 19 个 Repository，判断是启动时编译产物不完整，建议 IDEA Build → Rebuild Project 后重启。`.env` 未被 Spring Boot 4.1.1 加载（jar 内无 dotenv 支持），`@ConfigurationProperties` 绑定对不可解析占位符是"容忍"的，于是绑定到字面量 `${REFRESH_TOKEN_PEPPER}`（23 字符）触发长度校验。IDEA 运行配置里也没有 env 变量。结论：必须用 `set -a && source .env && set +a` 后从该 shell 启动 IDEA，或在运行配置里配 env / 装 EnvFile 插件。同类隐患：DB_URL/DB_USERNAME/DB_PASSWORD 会在 DataSource 初始化时报占位符错误，JWT_SECRET_BASE64 字面量会在 JwtTokenService 构造时报 "must be valid Base64"。
